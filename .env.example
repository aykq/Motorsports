# Database
POSTGRES_DB=motorsports
POSTGRES_USER=ms_user
POSTGRES_PASSWORD=change_this_strong_password

# Auth.js
AUTH_SECRET=                    # openssl rand -base64 32
AUTH_GOOGLE_ID=                 # Google Cloud Console > OAuth 2.0 Client ID
AUTH_GOOGLE_SECRET=             # Google Cloud Console > OAuth 2.0 Client Secret
AUTH_RESEND_KEY=                # resend.com dashboard > API Keys (gönderici: onboarding@resend.dev)

# Web Push (VAPID)
VAPID_PUBLIC_KEY=               # npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=   # same as VAPID_PUBLIC_KEY (client-side access)
VAPID_PRIVATE_KEY=              # npx web-push generate-vapid-keys
VAPID_EMAIL=mailto:your@email.com

# Cron güvenlik
CRON_SECRET=                    # openssl rand -base64 32

# Admin (kullanıcı onay sistemi)
DISCORD_WEBHOOK_URL=            # Discord kanal ayarları > Integrations > Webhooks
ADMIN_EMAIL=                    # Onay sayfasına erişecek hesabın Google email'i
ADMIN_SECRET=                   # openssl rand -hex 20

# Geliştirme: passwordless login (sadece .env.local'da, production'da kesinlikle ekleme)
# ENABLE_DEV_LOGIN=1

# Uygulama
NEXT_PUBLIC_APP_URL=https://your-domain.com

# Gemini API (yarış olayları çevirisi)
GEMINI_API_KEY=                 # aistudio.google.com/app/apikey
GEMINI_MODEL=

# Drizzle ORM (otomatik oluşturulur, değiştirme)
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
